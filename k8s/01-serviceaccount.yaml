apiVersion: v1
kind: ServiceAccount
metadata:
  name: ticketing-sa
  namespace: ticketing
# Least-privilege: aplikacija ne treba pristup Kubernetes API-ju,
# pa se token namjerno NE montira u podove.
automountServiceAccountToken: false
